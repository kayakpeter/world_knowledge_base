__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
dist/
build/
.env

# Data files — track structure not runtime data
data/raw/*.parquet
data/processed/*.parquet
data/graph/financial_kg.json

# Lambda upload package
*.tar.gz
lambda_package/
.worktrees/

# Strategist disposable outputs — per-scenario deltas + orphan current.json,
# zero production consumers (only strategist tests reference them).
data/regime_config/delta_*.json
data/regime_config/current.json
