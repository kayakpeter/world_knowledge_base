"""Components — swappable modules behind the contracts. DESIGN.md §5.

Each is a Protocol so a field advance (a new world model, a new value formulation)
can drop in without disturbing the others, as long as it honors its contract.
"""
