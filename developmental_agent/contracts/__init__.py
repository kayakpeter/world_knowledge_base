"""Contracts — the stable seams. DESIGN.md §2, §5.

These define what flows across module boundaries. They are part of the schema:
changing them is expensive; changing the components behind them is not.
"""
