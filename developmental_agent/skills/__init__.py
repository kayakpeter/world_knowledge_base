"""Skills — the developmental curriculum, ascending. DESIGN.md §6, §9, §14.

Each skill is a learned function over the SAME fixed I/O surface; later skills only
populate the agent's internal model. A skill may not be built upon until its gate
passes (test-and-verify, never break-and-move-fast).

  skill0_agency  -- forward model + self/world + controllability   (gate M1-M5)
  skill1_reach   -- drive seed + reach->grasp->mouth + critic       (gate N1-N5, C1-C4, W1/C-corr)
  (next) skill2  -- object examination: rotate / probe hardness / place  -- ADDS A DRIVE
"""
